export const functions = {
    data: function () {
        return {
        };
    },
    computed: {},
    methods: {
        format_price: function(value) {
            return format_price(value);
        },
    }
}
